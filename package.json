{
  "name": "pirate-deck",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:styles": "node scripts/build-foundation-css.mjs",
    "dev": "npm run prepare:styles && astro dev",
    "build": "npm run prepare:styles && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.13.2"
  }
}
