{
  "name": "pirate-deck",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:styles": "node scripts/build-foundation-css.mjs",
    "prepare:scripts": "node scripts/build-ui-runtime.mjs",
    "prepare:runtime": "npm run prepare:styles && npm run prepare:scripts",
    "verify:sources": "node scripts/verify-runtime-sources.mjs",
    "verify:styles": "node scripts/verify-style-sources.mjs",
    "verify:assets": "node scripts/verify-asset-integrity.mjs",
    "verify:release": "node scripts/verify-release-integrity.mjs",
    "verify:pages": "node scripts/verify-pages-deploy.mjs",
    "verify:runtime": "npm run verify:sources && npm run verify:styles && npm run verify:assets && npm run verify:release",
    "dev": "npm run prepare:runtime && npm run verify:runtime && astro dev",
    "build": "npm run prepare:runtime && npm run verify:runtime && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.13.2"
  }
}
